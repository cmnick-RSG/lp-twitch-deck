# Python
.venv/
__pycache__/
*.pyc

# Secrets — NEVER commit. Use env vars / Vercel secrets instead.
*service*account*.json
ai-labs-rsg-*.json
*.key
.env
.env.*

# Collected data (regenerated by collectors)
data/

# Playwright browser cache
ms-playwright/

# Dev artifacts (screenshots from verify scripts)
site_*.png
*.png
_shots/
# …but DO ship the game-branded web assets
!site/public/assets/
!site/public/assets/*.png

# Local tool config
.claude/

# Internal / sensitive docs — do NOT publish
team sync transcript.txt
*transcript*.txt
*.json.bak

# Real-time live feed (served by /api/live in prod; regen locally via collect_live.py)
site/public/live.json

# Downloaded UI/UX skill (local design reference only, not part of the app)
ui-ux-pro-max-skill-main/
